{
  "name": "parrotizer",
  "version": "0.0.1",
  "description": "Start party on any web pages.",
  "scripts": {
    "dev": "webextension-toolbox dev",
    "build": "webextension-toolbox build"
  },
  "devDependencies": {
    "webextension-toolbox": "latest"
  }
}
